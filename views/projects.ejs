<%- include('partials/layout-top') %>

<section class="page-intro">
  <h1><%= page.title %></h1>
  <% if (page.intro) { %><p><%= page.intro %></p><% } %>
  <a class="button" href="/your-photos">Your Photos</a>
</section>

<section class="project-list">
  <% galleries.forEach(function(g) { %>
    <a class="project-card" href="/projects/<%= g.slug %>">
      <div class="project-card-thumb">
        <% if (g.images.length > 0) { %>
          <img src="<%= g.images[0].src %>" alt="<%= g.images[0].alt %>" loading="lazy" />
        <% } else { %>
          <div class="project-card-thumb-empty"></div>
        <% } %>
      </div>
      <h2 class="project-card-title"><%= g.title %></h2>
      <p class="project-card-description"><%= g.description %></p>
    </a>
  <% }); %>
</section>

<%- include('partials/layout-bottom') %>
